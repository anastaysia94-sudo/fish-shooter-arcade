# Hierarchy v2 release candidate

Database migrations are applied, Edge Function provisioning is deployed with JWT verification, live rollback smoke tests pass, and branch CI must be green before merge. See `HIERARCHY_V2_VERIFICATION.md`.
