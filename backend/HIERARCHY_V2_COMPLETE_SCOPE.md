# Hierarchy v2 completed scope

The production hierarchy implemented by this release is **Founder → Distributor → Agent → User**. Its server-side invariants and live smoke-test evidence are documented in `backend/README.md` and `backend/HIERARCHY_V2_VERIFICATION.md`.
