name: F.S.A. Low-Data Budget

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]
    paths:
      - "index.html"
      - "fsa-v8.css"
      - "gameplay-layout-v9.css"
      - "fsa-v9.js"
      - "sw.js"
      - "guide.html"
      - "robots.txt"
      - "sitemap.xml"
      - "tests/low-data-budget.mjs"
      - "tests/low-data-behavior.mjs"
      - ".github/workflows/low-data-budget.yml"

permissions:
  contents: read

jobs:
  low-data-budget:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Enforce 2G cold-start source and offline-shell budgets
        run: node tests/low-data-budget.mjs
      - name: Enforce low-data and 2G behavioral contracts
        run: node tests/low-data-behavior.mjs
