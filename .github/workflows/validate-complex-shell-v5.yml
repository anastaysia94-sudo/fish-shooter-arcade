name: Legacy F.S.A. v5 Shell Validator
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy v5 shell validator retained for manual historical checks; current release is F.S.A. v8.'
